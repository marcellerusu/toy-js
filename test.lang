import fs from "fs"
import Lexer from "./lexer"

@type A = num

dataclass Obj(a: A, b: str, c: Array[str])

let d = fs.readFileSync("./test.lang").toString()

let [, , file_name, option] = process.argv

console.log(file_name ++ "sf")
let b = new Obj(10, "str", ["wtf"])

let a = b.a
